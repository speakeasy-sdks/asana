/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import { SpeakeasyBase, SpeakeasyMetadata } from "../../../internal/utils";
import { ProjectCompact } from "./projectcompact";
import { UserCompact } from "./usercompact";
import { Expose, Type } from "class-transformer";

/**
 * Whether the user has full access to the project or has comment-only access.
 */
export enum ProjectMembershipResponseWriteAccess {
    FullWrite = "full_write",
    CommentOnly = "comment_only",
}

/**
 * With the introduction of “comment-only” projects in Asana, a user’s membership in a project comes with associated permissions. These permissions (whether a user has full access to the project or comment-only access) are accessible through the project memberships endpoints described here.
 */
export class ProjectMembershipResponse extends SpeakeasyBase {
    /**
     * Globally unique identifier of the resource, as a string.
     */
    @SpeakeasyMetadata()
    @Expose({ name: "gid" })
    gid?: string;

    @SpeakeasyMetadata()
    @Expose({ name: "project" })
    @Type(() => ProjectCompact)
    project?: ProjectCompact;

    /**
     * The base type of this resource.
     */
    @SpeakeasyMetadata()
    @Expose({ name: "resource_type" })
    resourceType?: string;

    @SpeakeasyMetadata()
    @Expose({ name: "user" })
    @Type(() => UserCompact)
    user?: UserCompact;

    /**
     * Whether the user has full access to the project or has comment-only access.
     */
    @SpeakeasyMetadata()
    @Expose({ name: "write_access" })
    writeAccess?: ProjectMembershipResponseWriteAccess;
}
